import makeStyles from "@material-ui/core/styles/makeStyles";

export const MaterialStepperStyles = makeStyles((theme) => ({
    fullWidthGridItem: {
        width: "100%"
    },
    processBarStepElement: {
        marginLeft: "1rem",
        marginRight: "1rem"
    }
}));